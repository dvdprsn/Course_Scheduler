image: node:latest

stages:
    - build
    - run

first-job:
    stage: build
    script:
        - cd frontend/
        - npm install
    artifacts:
        paths:
            - node_modules

second-job:
    stage: run
    script:
        - cd frontend/
        - npm run start
        - node test.js
    artifacts:
        paths:
            - node_modules/